export function ProductFilters() {
  return (
    <div className="editorial-panel border-t-[3px] border-t-[var(--foreground)] p-6">
      <p className="page-kicker">Filter</p>
      <h3 className="mt-3 text-2xl font-semibold tracking-tight">Product Filters</h3>
      <p className="mt-2 text-sm text-[var(--muted)]">
        This is the placeholder area for search, finish, size, and category filters.
      </p>
    </div>
  );
}
